# Tuple is used to just print the values no other function is used in Tuple.
# cannot be changed(immutable) and  () brackets are used. Has ordered collection(display in same format)
t=(23,45,78,10.9,67)
print(t[0])
print(type(t))
print(t[0:4])
